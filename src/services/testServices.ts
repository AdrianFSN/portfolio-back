/* import sendTextRequest from "./requesters/resizeThumbnailRequest.js";

sendTextRequest("Hello, cote!", (error, result) => {
  if (error) {
    console.error("Request failed:", error);
  } else {
    console.log("Response received:", result);
  }
}); */
